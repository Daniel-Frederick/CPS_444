package main
import (
	"fmt"
	"math"
)

func main() {
	if (len(os.Args) < 2) {
		fmt.Println("No Name given!")
		return;
	}
	if (len(os.Args) > 3) {
	  fmt.Println("Only enter one name!")
		return;
	}


}
